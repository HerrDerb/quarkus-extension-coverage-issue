pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'io.quarkus.extension' version '2.15.3.Final'
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

include 'runtime', 'deployment'
